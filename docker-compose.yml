name: renovation-crm

services:
  db:
    image: pgvector/pgvector:pg15
    restart: always
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: password
      POSTGRES_DB: crm
    volumes:
      - postgres_data:/var/lib/postgresql/data
    healthcheck:
      test: [ "CMD-SHELL", "pg_isready -U postgres -d crm" ]
      interval: 10s
      timeout: 5s
      retries: 5
    networks:
      - default

  redis:
    image: redis:alpine
    restart: always
    volumes:
      - redis_data:/data
    healthcheck:
      test: [ "CMD", "redis-cli", "ping" ]
      interval: 10s
      timeout: 5s
      retries: 5
    networks:
      - default

  app:
    build: .
    restart: always
    env_file: .env
    expose:
      - "8000"
    labels:
      - "traefik.docker.network=hzxkc651dtomqyna2ibeuaxl"
      - "traefik.http.services.app.loadbalancer.server.port=8000"
    networks:
      - default
    depends_on:
      db:
        condition: service_healthy
      redis:
        condition: service_healthy
    healthcheck:
      test: [ "CMD", "curl", "-f", "http://localhost:8000/health/ready" ]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 20s

  worker:
    build: .
    command: python run_worker.py
    restart: always
    env_file: .env
    depends_on:
      app:
        condition: service_healthy
      db:
        condition: service_healthy
      redis:
        condition: service_healthy
    networks:
      - default

  userbot:
    build: .
    command: python run_user_bot.py
    restart: always
    env_file: .env
    depends_on:
      app:
        condition: service_healthy
      db:
        condition: service_healthy
      redis:
        condition: service_healthy
    networks:
      - default

  frontend:
    build:
      context: ./frontend
    restart: always
    expose:
      - "80"
    labels:
      - "traefik.docker.network=hzxkc651dtomqyna2ibeuaxl"
      - "traefik.http.services.frontend.loadbalancer.server.port=80"
    depends_on:
      app:
        condition: service_healthy
    networks:
      - default

volumes:
  postgres_data:
  redis_data:
