name: renovation-crm

services:
  db:
    image: pgvector/pgvector:pg15
    restart: always
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: password
      POSTGRES_DB: crm
    volumes:
      - postgres_data:/var/lib/postgresql/data
    healthcheck:
      test: [ "CMD-SHELL", "pg_isready -U postgres -d crm" ]
      interval: 10s
      timeout: 5s
      retries: 5

  redis:
    image: redis:alpine
    restart: always
    volumes:
      - redis_data:/data
    healthcheck:
      test: [ "CMD", "redis-cli", "ping" ]
      interval: 10s
      timeout: 5s
      retries: 5

  app:
    build: .
    restart: always
    env_file: .env
    expose:
      - "8000"
    volumes:
      - media_data:/app/media
    depends_on:
      db:
        condition: service_healthy
      redis:
        condition: service_healthy
    healthcheck:
      test: [ "CMD", "curl", "-f", "http://localhost:8000/health/ready" ]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 20s

  worker:
    build: .
    command: python run_worker.py
    restart: always
    env_file: .env
    volumes:
      - media_data:/app/media
    depends_on:
      app:
        condition: service_started
      db:
        condition: service_healthy
      redis:
        condition: service_healthy

  userbot:
    build: .
    command: python run_user_bot.py
    restart: always
    env_file: .env
    volumes:
      - media_data:/app/media
    depends_on:
      app:
        condition: service_started
      db:
        condition: service_healthy
      redis:
        condition: service_healthy

  frontend:
    build:
      context: ./frontend
    restart: always
    expose:
      - "80"
    labels:
      - "traefik.enable=true"
      - "traefik.http.services.quiz-frontend.loadbalancer.server.port=80"
      - "traefik.http.routers.http-quiz-frontend.entryPoints=http"
      - "traefik.http.routers.http-quiz-frontend.rule=Host(`quiz.isaevremont.ru`) && PathPrefix(`/`)"
      - "traefik.http.routers.http-quiz-frontend.middlewares=redirect-to-https"
      - "traefik.http.routers.http-quiz-frontend.service=quiz-frontend"
      - "traefik.http.routers.https-quiz-frontend.entryPoints=https"
      - "traefik.http.routers.https-quiz-frontend.rule=Host(`quiz.isaevremont.ru`) && PathPrefix(`/`)"
      - "traefik.http.routers.https-quiz-frontend.middlewares=gzip"
      - "traefik.http.routers.https-quiz-frontend.service=quiz-frontend"
      - "traefik.http.routers.https-quiz-frontend.tls=true"
      - "traefik.http.routers.https-quiz-frontend.tls.certresolver=letsencrypt"
    depends_on:
      app:
        condition: service_started

volumes:
  postgres_data:
  redis_data:
  media_data:
